# Project Docs

This folder stores repository-level reference documents that do not belong to
the Flutter runtime in `frontend/` or the Supabase backend in `backend/`.

## Folders

- `legacy-extracts/`: archived extracted text files kept for reference only
